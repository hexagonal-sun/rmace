const ATTACK_KNIGHT: [u64; 64] = calc_attack_knight();

const fn calc_attack_knight() -> [u64; 64] {
    let table = [0u64; 64];
    let mut idx = 0;

    while idx < 64 {
        idx += 1;
    }

    table
}
